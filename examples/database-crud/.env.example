# Database Configuration Example

# Database type: sqlite, postgres, or memory
DATABASE_TYPE=sqlite

# Database URL
# For SQLite file:
DATABASE_URL=sqlite:crud_example.db

# For SQLite in-memory:
# DATABASE_URL=sqlite::memory:

# For PostgreSQL:
# DATABASE_URL=postgres://user:password@localhost:5432/dbname

# Database API configuration (for client-server setup)
DATABASE_API_URL=http://localhost:3000/api/db

# Authentication token for database API
# Generate a secure token in production!
DATABASE_API_TOKEN=your-secure-token-here

# Server configuration
HOST=127.0.0.1
PORT=3000